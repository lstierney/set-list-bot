services:
  setlistbot:
    image: ghcr.io/lstierney/setlistbot:latest
    container_name: setlistbot
    ports:
      - "7777:8080"
    env_file:
      - .env
    volumes:
      - "${AUDIO_BASE_PATH}:/audio:ro"
    restart: unless-stopped

